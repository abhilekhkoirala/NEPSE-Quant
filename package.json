{
  "name": "nepse-trading-bot",
  "private": true,
  "version": "1.0.0",
  "description": "Root convenience scripts only — see backend/ and frontend/ for the actual apps.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
